This bash script that is designed to start the running of the server currently only works when a harddisk is plgged in that mounts with "sudo mount /dev/sda1 /mnt/tbdrive/" and that drive should have a folder in root called video that recorded video files can be copied to.

In the future It is better if the sender pi optionally records the video locally, rather than it being done on the server.
